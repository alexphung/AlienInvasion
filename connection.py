
class Connection():
	""" This Connection class object are intended to allow the user to establish
		all type of connection to be use in the developing application and tools. """
		
    def __init__(self, connStr, type, protocol ):
		# Constructor to initialize the Connection Properties.
		self.ConnectionString = connStr
		self.ConnectionType = type
		self.ConnectionProtocol = protocol
		
	def Connect(self, type, protocol)
		# Establish the connection using the specified parameters.
		self.ConnectType = type
		self.ConnectionProtocol = protocol
		self.Connect
		